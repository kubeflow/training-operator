apiVersion: v1
kind: Service
metadata:
  labels:
    app: dashboard
  name: dashboard
spec:
  ports:
  - port: 80
    targetPort: 8080
  selector:
    app: dashboard
  type: LoadBalancer
---
apiVersion: extensions/v1beta1
kind: Deployment
metadata:
  labels:
    app: dashboard
  name: dashboard
spec:
  template:
    metadata:
      labels:
        app: dashboard
    spec:    
      containers:
      - name: dashboard
        image: wbuchwalter/tf_operator_dashboard:v20171104-8b2190d-dirty-16457ec
        ports:
        - containerPort: 8080 