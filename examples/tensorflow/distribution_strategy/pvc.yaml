apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: strategy-volume
  labels:
    app: strategy-volume
spec:
  storageClassName: "Your storageClassName"
  accessModes:
  - ReadWriteMany
  resources:
    requests:
      storage: 10Gi
